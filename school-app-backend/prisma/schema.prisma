generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
   // Use existing DB as shadow to avoid create permission
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String?
  createdAt DateTime @default(now())
}

model Grade {
  id        Int     @id @default(autoincrement())
  student   String
  subject   String
  score     Float
  createdAt DateTime @default(now())
}
